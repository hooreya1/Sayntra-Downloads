# Sayntra 보안 정책

## 지원 버전

| 버전 | 상태 |
|---|---|
| 최신 `0.4.x` 공개 베타 | 보안 수정 검토 대상 |
| 그보다 오래된 베타 | 새 버전에서만 수정될 수 있음 |

Sayntra는 아직 베타이며 정해진 보안 지원 기간이나 응답 SLA를 제공하지 않는다.

## 비공개 취약점 신고

민감한 취약점은 공개 Issue에 작성하지 말고 다음 GitHub 비공개 신고 경로를 사용한다.

<https://github.com/hooreya1/Sayntra-Downloads/security/advisories/new>

보고서에는 가능한 경우 다음을 포함한다.

- 영향을 받는 Sayntra 버전
- Windows 빌드와 권한 수준
- 재현 단계와 기대/실제 결과
- 영향 범위
- 민감 콘텐츠를 제거한 오류 코드 또는 최소 증거

원본 음성, 실제 전사문, 클립보드 내용, 개인 용어, 창 제목과 계정 정보를 첨부하지
않는다. 재현에 텍스트가 필요하면 임의의 비민감 테스트 문장을 사용한다.

일반 사용 오류와 기능 제안은 [SUPPORT.md](SUPPORT.md)의 공개 Issue 경로를 사용한다.

## 우선 확인하는 보안 경계

- 포커스 변경 후 잘못된 창으로 자동 붙여넣기
- 오래되거나 취소된 작업 결과의 반영
- 합성 키·마우스 입력에 의한 녹음 또는 취소
- 로그·임시 파일·크래시 경로의 음성/전사/클립보드 노출
- 설치·업그레이드·자동 시작 레지스트리 소유권
- 모델 및 설치 산출물 공급망 변조
- 일반 권한 앱과 관리자 권한 앱의 입력 경계

## 배포 무결성의 현재 한계

- 공개 `0.4.1` 설치기는 Authenticode 서명이 없다. Windows가 알 수 없는 발행자 또는
  SmartScreen 경고를 표시할 수 있다.
- 릴리스의 SHA-256 sidecar는 전송 오류와 우발적 변형 확인에는 유용하지만, 설치기와
  sidecar가 함께 교체되는 저장소 침해에 대한 진위성 증명은 아니다.
- 자동 업데이트가 없으므로 사용자가 공식 Releases에서 수동으로 새 버전을 확인한다.
- Whisper 모델은 저장소와 40자 commit revision을 고정하고, 내장된 파일 크기와
  SHA-256 목록으로 최초 준비 시 전체 파일을 검증한다. 이후에는 같은 파일의
  크기·시간·파일 식별자와 manifest가 일치하는 로컬 검증 표식을 사용해 약 1.6 GB를
  매번 다시 해시하지 않는다. 이는 동일 사용자 권한으로 로컬 캐시와 검증 표식을 함께
  바꿀 수 있는 공격까지 막는 서명 체계는 아니다.
- 모델 무결성 오류 뒤 사용자가 `다시 시도`를 선택하면 고정 repository·revision과
  검증 대상 파일만 강제로 다시 내려받아 재검증한다.

공개 범위를 넓히기 전 Authenticode 코드 서명과 서명된 릴리스 manifest를 도입하는
것이 릴리스 게이트다.

## 의존성 점검

2026-07-29의 잠금된 런타임 의존성 37개를 PyPI advisory 데이터베이스 기반
`pip-audit`으로 확인했으며 당시 알려진 취약점은 0건이었다. 이는 해당 시점과 해당
데이터베이스의 점검 결과일 뿐, 미래 취약점이 없다는 보증이나 네이티브 바이너리·모델
전체에 대한 독립 보안 감사를 뜻하지 않는다.
